# 2026-09-08

## 今日完成
- [x] **M9 长期记忆设计**（以提问引导方式完成，非直接给代码）：确定三层记忆语义（用户/项目/会话）、key 设计（`Storage` 之上加 `MemoryStorage` 门面层、key 按前缀分层）、项目层用可逆 URL 编码标识工作目录、注入链路走 `SESSION_START` 钩子的可变 `List` payload。
- [x] **新建 `mycc-memory` 模块**：`MemoryStorage`（三层读写 + 项目路径 URL 编码 + 构造器注入）、`MemoryType` 枚举、`MemoryHook`（SESSION_START 时把三层记忆追加进 system prompt list，构造器注入）。
- [x] **agent 链路改造**：`Message` 新增 `system()` 工厂方法；`AgentLoop.run()` 中会话开始时 new 空 list → 派发 `SESSION_START` 钩子 → 仅当订阅者确实追加了内容才 `addMessage(Message.system(...))`。
- [x] **装配激活**：`mycc-app` 依赖 `mycc-memory`；`MemoryHook` 为 `@Component` + `@Hook(SESSION_START)`，容器扫描根包 `com.learn.mycc` 时自动注册生效。
- [x] **测试补全（测试优先，先红后绿）**：
  - `MemoryStorageTest`（8 个）：三层读写往返、三层同 id 不串、未存返回 null、URL 编码落盘、可逆往返。
  - `MemoryHookTest`（3 个）：三层记忆进 list、无记忆跳过、payload null 不崩。
  - `AgentMemoryInjectionTest`（2 个，端到端）：记忆经 `AgentLoop` 注入 provider 请求的 SYSTEM 消息、无记忆时无 SYSTEM 消息。
  - 修正 `mycc-memory`/`mycc-agent` 的 pom（补 test 依赖）。

## 测试优先抓出的 bug（全部亲手修复）
- Windows 盘符 `:` 属非法文件名字符 → `encodeProjectPath` 改用 `URLEncoder`（原逐字符替换 `|` 会残留 `:` 导致 `InvalidPathException`）。
- save/load 项目层编码不对称 → 把编码收敛进 `key()`，读写共用。
- `replace("\\\\", "|")` 转义写错 → 改 `"\\"`（单个反斜杠）。
- 在 `AgentLoop` 预置硬编码 SYSTEM_PROMPT 导致 3 个既有测试失败 → 改为"仅当 hook 追加了内容才注入"，恢复全绿（路线乙）。

## 待办事项
- [ ] M9 记忆的"写入途径"尚未提供：目前 `saveMemory` 无调用方，需决定记忆由谁写入（agent 代码内某处自动 save，还是暴露自定义 `@Tool` 给 LLM 调）。
- [ ] `decodeProjectPath` 目前无调用方（装饰性代码），按 YAGNI 可考虑删除或明确用途。
- [ ] 全部测试的最终全项目回归（`mvn clean install`）与 CLI/Web 双界面可用性验证。

## 阻塞与风险
- 无阻塞。风险提示：M9 仅实现"读 + 注入"，若后续不补"写入途径"，三层记忆实际只会读到空内容，能力不闭环。

## 明日计划
- 确定记忆写入途径（agent 内 save / `@Tool` 暴露）。
- 处理 `decodeProjectPath` 去留。
- 跑全量 `mvn clean install` 回归，更新 v2 计划 M9 勾选状态。