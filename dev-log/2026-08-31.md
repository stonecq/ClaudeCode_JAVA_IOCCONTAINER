# 2026-08-31

## 今日完成
- [x] M4-1 新建 mycc-ui 模块（纯接口）：InteractionPort + OutputEvent（type/payload/sessionId/seq）+ OutputEventType
- [x] M4-1 新建 mycc-agent 模块：session/Session、Conversation、Message（会话状态与 ChatMessage 分离，可转换）；会话模型单测
- [x] M4-2 ToolCallExecutor：查 ToolRegistry → JSON 参数绑定 → 反射调用 → ToolResult（失败不抛，回填给 LLM）；测试：参数转换/未知工具/异常/缺参
- [x] M4-3 AgentLoop：多轮调 LLM → 工具调用执行并回填 → 无工具则结束；maxIterations 终止；Provider 错误转 ERROR 事件；工具失败回填
- [x] M4-4 流式事件：每轮下发 token/tool_call/tool_result/done；事件序列与 seq 递增测试
- [x] M4-5 端到端：MockProvider 脚本化驱动"写文件→读文件→最终回复"多轮，断言工具结果回填、历史完整、事件流完整
- [x] mycc-ai 三处配合改动：ChatMessage 扩展支持携带 tool_calls；OpenAiCompatProvider 回填 assistant tool_calls（多轮协议必需）；MockProvider 支持 scripted(ChatScript) + 异常走 onError
- [x] `mvn clean install` 全绿（71 测试 = 28 core + 21 tools + 9 ai + 12 agent + 1 app）
- [x] 交互式演示入口 AgentDemoApp + ConsolePort（mycc-app，exec 已指向）：Mock LLM 驱动"写入/读取 demo.txt"真实工具调用，流式输出；管道验证通过
- [x] 用户改造：AgentDemoApp 接入真实 DeepSeek（OpenAiCompatProvider + DEEPSEEK_API_KEY）；MyccApplication 改为包扫描 + Lombok；AgentLoop/Session 会话写入封装
- [x] 评审修复：移除 Lombok（JDK26 默认不自动发现注解处理器，@Getter 失效）改手写 getter；Session.addConversion 更名 addMessage 并统一 AgentLoop 四处调用；AgentDemoApp 补 DEEPSEEK_API_KEY 缺失友好提示；清理无用 import。71 测试全绿

## 待办事项
- [ ] M5：存储抽象（Storage SPI + FileStorage + ConfigService）+ 会话落盘
- [ ] M6：CLI（JLine + picocli）+ fat-jar

## 阻塞与风险
- Windows 控制台 GBK 输出乱码依旧只是显示问题（surefire 断言信息可见乱码，测试本身按 Unicode 判断），M6 CLI 需显式 UTF-8。
- 反射调用 @Tool 方法要求工具类 public（嵌套测试类踩过一次：包私有类的方法跨包反射不可访问）。

## 明日计划
- 进入 M5：Storage SPI + FileStorage（JSON 落盘 ~/.mycc/）+ ConfigService + 会话序列化（Conversation ↔ JSON）
