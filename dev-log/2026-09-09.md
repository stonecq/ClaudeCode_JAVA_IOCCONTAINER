# 2026-09-09

## 今日完成
- [x] **M9 二期：记忆索引 + 按需调取 + 回合自动保存**（以提问方式敲定 4 个设计决策：分层索引 / read·save·delete 工具集 / 主动保存+会话自动 / 会话层单条整体）。
- [x] **MemoryIndex（新）**：纯字符串索引工具（`- id: 描述` 行格式），`upsert`（同 id 更新描述保持位置 / 追加）、`remove`（清空返回空串）。7 单测。
- [x] **MemoryStorage 改造**：构造注入 `ApplicationConfig` 定位当前工作区；新增 USER/PROJECT 层的 `loadIndex / load / save / delete`（PSESSION 传入抛异常），SESSION 层专用 `loadSession / saveSession`（单条，不走索引）。PROJECT 层按 `memory/project/<enc(ws)>/` 命名空间隔离不同项目。旧 `saveMemory/loadMemory` 移除，不留 shim。17 单测。
- [x] **MemoryTools（新）**：`read_memory / save_memory / delete_memory` 三 `@Tool`（`@Component`，SESSION 层拒绝），承接「看索引→按 id 调取→写入」闭环。7 单测；经 ToolRegistry（BeanPostProcessor）自动装配，`MyccApplicationTest` 内置工具集合断言 7→10 佐证。
- [x] **MemoryHook 改造**：SESSION_START 注入「会话全文 + USER 索引 + PROJECT 索引」（每层带引导标题，空层跳过）；新增 SESSION_END 订阅 `saveTurnMemoryHook`，把回合记录写回会话层。6 单测。
- [x] **AgentLoop.run（agent 模块）**：SESSION_END payload 由 null 改为「用户: …\n回答: …」String（不引入跨模块类型，`mycc-agent` 主代码仍不依赖 mycc-memory）。
- [x] **测试更新**：重写 `AgentMemoryInjectionTest`（新 API + 索引注入断言 + 回合落盘端到端），`AgentLoopHookTest` 事件序列不受影响。
- [x] **M10 Skill 一期（注解方式 + Invoke）**：core 新增 `@Skill` 注解 + `SkillDefinition` + `SkillRegistry`（BeanPostProcessor，与 Tool/Hook 同构），`IocContainer` 挂 skillRegistry 三件套齐；新建 `mycc-skill` 模块（`SkillHook` SESSION_START 注入技能目录 + `SkillTools.invoke_skill` @Tool + `CodeReviewSkill` 示例）；`AgentLoop` 零改动、装配零改动（技能类 `@Component`+`@Skill` 自动进容器）。SKILL.md 文件加载留二期。
- [x] **M10 测试**：`SkillRegistryTest`（7，core）+ `SkillHookTest`/`SkillToolsTest`（mycc-skill 5）+ `SkillAssemblyTest`（agent 端到端：目录注入 + invoke_skill 取回指令 2）；`MyccApplicationTest` 工具集合断言 10→11。
- [x] **M10 二期（SKILL.md 文件加载）**：core 的 `SkillRegistry` 新增 `override`（同名替换，供"项目覆盖全局"）；mycc-skill 新增 `SkillFileParser`（frontmatter 键值解析，零 YAML 依赖，无成对 `---` 按全文即指令）与 `SkillFileLoader`（`@Component`+`InitializingBean`，容器启动时先扫全局 `~/.mycc/skills` 唯一注册、再扫项目 `<workspace>/.mycc/skills` 覆盖注册；空目录静默跳过、不热更新）。文件技能与注解技能共享注册表，`invoke_skill`/技能目录自动覆盖，零额外接线。
- [x] **M10 二期测试**：`SkillFileParserTest`（5）+ `SkillFileLoaderTest`（6：全局+项目叠加/项目覆盖/忽略无 SKILL.md 目录/空根不崩/与已有注册同名快速失败/组件空根加载）+ `SkillRegistry` override 3 测；修正了"全局内同名"用例（文件系统同名目录不可共存，改测"全局与已注册注解同名"真实语义）。
- [x] **M11 Planning 一期（工具驱动）**：core 新增 `ToolContext` 工具上下文注入（record + `ParameterSchemaGenerator` 跳过该参数 + `ToolCallExecutor` 执行时注入当前 sessionId——工具首度感知会话，为 Subagent 复用打基础）；agent 新增 `planning` 包：`Plan`/`PlanStep`（不可变 record，markDone/allDone/nextIndex）、`PlanStore`（按 sessionId JSON 落盘 `plan/<id>.json`）、`PlanTools`（`create_plan`/`complete_step` 两 @Tool，步骤用多行 String 参数）；`AgentLoop.handleToolCalls` 改传 `ToolContext(session.id())`。计划文本经工具结果自然进入消息历史，LLM 逐步推进，无需注入 Hook。
- [x] **M11 测试**：core `ParameterSchemaGeneratorTest`/`ToolCallExecutorTest` 扩展（schema 跳过 ToolContext + 执行注入）；agent planning `PlanTest`(4)/`PlanStoreTest`(4)/`PlanToolsTest`(6)/`PlanFlowTest`(端到端：建计划→逐步 complete_step→全部完成)；`MyccApplicationTest` 工具集合断言 11→13。
- [x] **M11 收尾（计划完成清理）**：`PlanStore` 新增 `delete`；`complete_step` 全部完成时删除计划文件（`allDone` 判定后直接删、不先 save），端到端确认同会话不再残留。修复"计划文件残留"缺口：避免随会话累积的垃圾文件 + 续聊读到旧已完成计划的语义混乱。
- [x] **M11 补充（read_plan 读取计划）**：`PlanTools` 新增 `read_plan` @Tool（修正用户草稿的 `read_tool` 命名），无计划时返回提示文本而非抛错（查询无结果非失败）；补 3 单测（有计划全文 / 无计划提示 / 全完成回落提示），`MyccApplicationTest` 工具断言 13→14。
- [x] **M12 Subagent 子代理**：`SubagentService`（@Component）注入 LlmProvider/ToolRegistry/InteractionPort/ConfigService，子代理工具集限定文件检索白名单 7 个（不含 subagent——天然防递归），新建临时 Session 预置系统提示、嵌套 `AgentLoop.run(task)`、storage=null 不落盘、复用主 port（sessionId 区分）；`SubagentTools` 暴露 `subagent` @Tool；主循环与装配零改动。
- [x] **M12 测试**：`SubagentServiceTest`(4：白名单过滤 / 默认提示词 / 自定义提示词 / 空工具纯文本)、`SubagentToolsTest`(1)、`SubagentFlowTest`(端到端：区分主/子 conversationId，子代理结果回填主会话 + 工具声明含 subagent + 输出复用 port)；`MyccApplicationTest` 工具断言 14→15。期间修正 flow 测试设计（service 须与主循环共享同一 provider）。
- [x] **M12 重构（subagentExcluded 声明式）**：按用户反馈把子代理工具开放从 `SubagentService.WHITELIST` 硬编码列表改为 `@Tool` 声明——新增 `@Tool.subagentExcluded() default false`（默认允许子代理调用），`ToolDefinition` 承载 `isSubagentExcluded()`，`ToolRegistry` 注册捕获；`SubagentService` 改为「排除过滤」。`subagent`（防递归）+ memory/skill/plan 工具标 `subagentExcluded=true`，文件/检索工具保持默认开放，行为与白名单等价（子代理仍专注执行）。`app` 工具断言不变（15）。
- [x] **重构测试**：`ToolRegistryTest` 补 `capturesSubagentExcludedFlagFromAnnotation`（excluded=true / 默认 false）；`SubagentServiceTest` 语义从「白名单」改「排除过滤」（fixture 标注 excluded）。
- [x] **M11/M12 模块化**：把 planning 与 subagent 从 mycc-agent 拆为独立模块（对齐 memory/skill 惯例）——新 `mycc-planning`（依赖 core+storage，包 `com.learn.mycc.planning`，测试含端到端自建 RecordingPort）与新 `mycc-subagent`（依赖 core/ai/ui/storage + **agent**——`SubagentService` 嵌套 `AgentLoop` 需要的单向依赖，包 `com.learn.mycc.subagent`）；测试随类迁移，agent 移除原 planning/subagent 代码（测试数 78→51）；父 pom modules、app 依赖各 +2；工具断言 15 不变。
- [x] **M9b 记忆清理（专用子代理 + 回合结束自动）**：`SubagentService` 新增 `run(task, prompt, allowedTools)` 指定工具集重载（按名覆盖 subagentExcluded）；`MemoryStorage` 新增 `entryCount`；新 `MemoryCleanupHook`（SESSION_END 检测 USER/PROJECT 层条数超上限（默认 20，可配 `memory.maxEntriesPerLayer`）→ 启动专用清理子代理装配 read/save/delete_memory 整理）。清理编排放 mycc-subagent（subagent→memory 无环），memory 保持纯机制层。期间修正：`getDeclaredMethods` 反射顺序不保证源码序 → 工具集断言改 `containsExactlyInAnyOrder`。
- [x] **M9b 完善（load_index 工具 + 注解驱动清理工具集）**：新增 `load_index` @Tool（清理子代理未随会话注入索引时主动拉取各层索引，空索引友好提示）；`@Tool` 增 `memoryCleanAgentExclude`（默认 true）+ `ToolDefinition` 承载 + getter；`MemoryCleanupHook` 改为注入 `ToolRegistry`、从 `!isMemoryCleanAgentExclude()` 推导清理子代理工具集（删除硬编码名单），阈值恢复默认 20（测试改用注入小阈值，不耦合默认值）；`MyccApplicationTest` 工具断言 15→16。
- [x] **Context Compact（四步压缩管线 + reactive）**：新建 `mycc-compact` 模块——`Compactor` 在 `ChatMessage` 层工作（不依赖 agent，无循环依赖），按"信息损失/成本递增"实现：① tool_result_budget（最新批结果超 20 万字符时把 >3 万的结果整体转存留路径+2000 预览）② snip_compact（>50 条时归档中间段、保留 3 头 + 46 尾 + 标记，切点保护 tool 调用↔结果配对）③ micro_compact/fit（旧结果落盘占位、保留最近 3）④ compact_history（调模型生成事实摘要、单条替换）。`AgentLoop` 接入：每轮调前 prepare 并回写会话、API 报 `prompt_too_long` 等时 reactive 压缩重试 1 次；新增 `Conversation.replaceAll` + `Message.fromChatMessage`；`AgentConfig` 注入 `Compactor`；转存/transcript 落 `<workspace>/.mycc/compact/`。`compact` 主动工具留二期。
- [x] **测试**：`CompactorTest`(7) + `CompactionFlowTest`(3 端到端：未超限不误触发 / 长历史截断 / reactive 重试) + `ConversationTest` 补 3；`MyccCommandTest` 补 `Compactor`/`ApplicationConfig` 装配。
- [x] **统一工作区 `.mycc` 读写接口（复用 FileStorage）**：`StorageConfig` 增 `@Bean WorkspaceStorage`——用**独立类型**（同类型双 bean 会因容器按 Class 唯一注册而冲突；返回 `Storage` 接口又会误导 `PlanStore` 注入），内部委托指向 `<ws>/.mycc` 的 `FileStorage`；`SkillFileLoader` 改为注入 `FileStorage`+`WorkspaceStorage`、按 `keys()` 扫描（删除自拼路径与 `Files.list`），`Compactor` 落盘改走 `WorkspaceStorage.write`、`SkillFileParser` 开放 `parse(name, content)`；`mycc-skill` pom 补 storage 依赖、`MyccCommandTest` 手工装配补 `StorageConfig`。
- [x] **配置整合（config.json + ConfigDefaults）**：`ConfigService` 改读 `~/.mycc/config.json`（嵌套 JSON 展平为点号 key；首次运行按默认自动生成一份）；新增 `ConfigDefaults` 集中**全部 16 个可配置项**的默认值（agent.model/maxIterations/maxReactiveRetries、cli.showReasoning、memory.maxEntriesPerLayer、compact 的 resultBudget/largeResult/maxMessages/contextLimit/microKeepRecent/reactiveKeep/targetRatio/preview/fitPreview/microMin/headKeep）；key 统一点命名空间并加 `getString/getInt/getBool/getDouble`；各模块删除自带 `DEFAULT_*`（消除 model 3 处、maxIterations 2 处重复）、调用点改用 `ConfigDefaults` 常量；`AgentLoop` reactive 重试次数改为构造参数（来自配置）；环境变量映射改为 `.`/驼峰边界→`_` 大写。
- [x] **workpath 可配置**：新增配置项 `workpath`（默认 `.`，即当前目录）——`ApplicationConfig` 去掉 `@Component`，改由 `StorageConfig` 的 `@Bean applicationConfig(ConfigService)` 依配置装配（core 不依赖 storage，故工厂放 storage）；工具/记忆/技能的作用目录现可经 config.json 指定。
- [x] **全量回归**：`mvn clean install` 通过，15 模块 346 测试全绿。

## 待办事项
- [ ] `decodeProjectPath` 除编解码往返测试外仍无调用方，按 YAGNI 决定去留。
- [ ] 记忆工具的权限：`@Tool` 默认 LOW 未排审批，`delete_memory` 有破坏性，是否设更高风险待定。
- [x] CLI 实机验证（部分）：已实测大模型会调用 `save_memory` 工具。
- [ ] CLI 实机验证：会话开始 SYSTEM 消息含三层记忆索引文本（用户/项目索引 + 会话全文），另确认回合结束 SESSION_END 实际落盘会话记忆。
- [ ] **记忆清理（已排期）**：数量阈值检测 + 回合结束触发清理，已列入 v2 计划「v2 后续增强 → M9b」，触发点/策略待启动时细化。

## 阻塞与风险
- 无阻塞。
- 已知边界：SESSION 层注入只在 SESSION_START（新建空会话）触发，同会话续聊不会重复注入（上下文由历史消息承载）；会话记忆是「用户输入 + 最终回答」的原文记录，**非语义摘要**（选定"不额外调 LLM"的自然结果）。

## 明日计划
- 处理 `decodeProjectPath` 去留与记忆工具权限等级。
- CLI 实机验证记忆注入与工具调用链路。
- M10 收尾可选：CLI 实测技能目录注入与 invoke_skill 调用；SKILL.md 文件加载（M10 二期）启动时再细化。
- M11 Planning 规划或按用户节奏推进。