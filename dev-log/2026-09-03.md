# 2026-09-03

## 今日完成
- [x] 规划 v2 阶段：保留 PRD + 新建 `docs/plans/2026-09-03-v2-development-plan.md`（M7–M13 路线图 = Hook → 权限 → Memory → Skill → Planning → Subagent → Web），M7 细化到任务级
- [x] 文档整理区分 v1/v2：v1 计划加「✅ 已完成」横幅 + DoD 勾选；PRD 状态行与 §7 标注 v1 完成/v2 进行中；`docs/README.md` 新增版本状态表；`CLAUDE.md` 同步 v2 阶段（简介/标准文档表/纪律 #1 M7→M13/模块地图 mycc-web 标 M13）
- [x] M7 核心（mycc-core `hook` 包）：`HookEventType`（6 事件枚举 + `eventName()`/`fromName()`）、`HookEvent`（type/sessionId/payload record）、`HookDefinition`、`HookRegistry`（BeanPostProcessor，EnumMap 按事件分组，签名校验启动期报错）、`HookDispatcher`（按注册顺序同步派发，单钩子异常仅 warn 不中断）
- [x] M7 装配：`IocContainer` 新增 `getHookRegistry()` + 构造挂入 hookRegistry 后置处理器；`AgentLoop` 新增 9 参工厂/构造（含 `HookDispatcher`，null=不触发钩子），埋点 session_start / session_end / user_prompt_submit / tool_call_before / tool_call_after / error
- [x] TDD：先写失败测试（HookEventTypeTest/HookRegistryTest/HookDispatcherTest/IocContainerHookTest/AgentLoopHookTest + fixture RecordingHook/BadEventHook/BadSignatureHook/OrderedHooks/HookRecorder）→ 实现 → 全绿
- [x] `mvn clean install` 全量验收全绿（39 core + 21 tools + 10 ai + 21 storage + 38 agent + 24 cli + 1 app = 154 用例）
- [x] 修正 `AgentLoop` SESSION_START 语义：仅「新建空会话」触发一次（`session.isEmpty()` 判空置于 addMessage 之前），续聊恢复的历史会话不重复宣告会话开始；SESSION_END 留在 finally 每轮触发。新增 `skipsSessionStartForResumedSession` 测试，`AgentLoopHookTest` 3 用例全绿。
- [x] **穿越校验前移到 hook 层**（用户决策：hook 实现校验，工具内部移除）：`mycc-hooks` 新增 `WorkspacePaths` 钩子（`@Component` + `@Hook(tool_call_before)`），normalize 后校验是否仍以工作区根为前缀拒绝绝对路径/越界；`ApplicationConfig` 改为双构造（no-arg + Path），`FileTools`/`SearchTools` 路径解析统一走 `config.getWorkspacePath()` 且不重复校验
- [x] **HookDecision 否决机制**（M7 扩展）：`HookDispatcher.dispatch` 返回 `HookDecision`，任一 allow=false 短路；`AgentLoop.handleToolCalls` 对被拒调用不执行、不触发 TOOL_CALL_AFTER，改以 `被钩子拦截: <reason>` 作为 TOOL 消息回流给 LLM
- [x] 新增测试隔离组件 `DenyEchoHook` fixture + `deniesToolCallBeforeExecution` 用例（验证工具未执行 + 拦截回馈）；`mycc-tools` 移除 traversal/absolute-path 拒绝用例（职责移交 hook 层），`WorkspacePathsTest` 7 用例在 `mycc-hooks` 模块
- [x] **CLI 运行时接线**：`CliContext` 新增 9 参构造（含 `HookDispatcher`，8 参委托 null 钩子）；`Main` 装配 `new HookDispatcher(container.getHookRegistry())` 传入；`mycc-app` 依赖 `mycc-hooks`（shade 后打入 fat-jar）；修正 `mycc-hooks/pom.xml` 误置的 source/target 26 → 规范父 pom（release 17）
- [x] 清理 `mycc-agent` 残留的 `SessionPicker.java`（M6 已删、本会话误重建为 AD 状态，无代码引用，`git rm` 移除）
- [x] `mvn clean install` 全量验收全绿（42 core + 17 tools + 10 ai + 21 storage + 39 agent + 24 cli + 7 hooks + 1 app = 161 用例），fat-jar 打入 hooks 模块
- [x] **`@Hook` 注解事件改为枚举类型**（用户要求：注解内写字符串太难看）：`event()` 属性由 `String` 改为 `HookEventType`，标注处一律 `@Hook(event = HookEventType.TOOL_CALL_BEFORE)`；删除 `HookEventType.fromName()` 字符串桥接与 `BadEventHook` fixture + `rejectsUnknownEventName` 测试（枚举类型把未知事件变成编译期错误，运行时校验冗余）；迁移全部 12 处标注点。全量验收全绿（156 core + 17 tools + 10 ai + 21 storage + 39 agent + 24 cli + 7 hooks + 1 app = 158 用例，净减 3 个失效测试，均为运行时校验用例）

## 待办事项
- [ ] M8：权限管理（工具调用审批流 + 决策持久化，走 M7 的 tool_call_before 钩子，可复用 HookDecision 否决）

## 阻塞与风险
- `mvn clean install` 首次因 Windows 文件锁（`mycc-app/target/mycc-app.jar` 被占用）失败，非代码问题；句柄释放后 clean 正常，全量构建 BUILD SUCCESS。
- HookDispatcher 当前仅同步执行；FR-8 的「异步执行」留待有需求时再扩展（YAGNI）。
- 钩子异常一律放行（fail-open）是明确取舍：安全校验钩子靠「可疑参数不落地到工具体」防绕过，简单优先；如需 fail-closed 再演进。

## 明日计划
- 进入 M8：权限管理——审批流（自动允许/拒绝/询问/规则）+ 高风险工具默认审批 + 决策持久化；先写 M8 详细 design/plan 再 TDD 实现，可复用 tool_call_before 钩子与 HookDecision 否决链路。
