# 2026-08-27

## 今日完成
- [x] 澄清需求：LLM 可插拔+Mock、自研 IoC、Maven+JDK17、Jetty+SSE、存储抽象、CLI 先行
- [x] 撰写 PRD：docs/superpowers/specs/2026-08-27-mycc-prd.md（开放项已定稿）
- [x] 建立文档目录：docs/{tech, design, standards, plans} 与开发日志规范 dev-log/
- [x] 编写 CLAUDE.md 工作指引（指向各标准文档）
- [x] 初始化 git 仓库与 .gitignore
- [x] 架构调整：UI 独立为 mycc-ui 模块（仅接口），核心/agent 面向接口，具体渲染在各 UI 模块，整体可插拔
- [x] M1-1 父 pom + mycc-core 模块骨架
- [x] M1-2 注解定义（@Component/@Inject/@Tool/@ToolParam/@Hook 预留）
- [x] M1-3 BeanDefinition + AnnotationScanner（类路径扫描）
- [x] M1-4 BeanFactory 依赖注入（构造器优先 + 字段兜底 + 循环依赖检测）
- [x] M1-5 BeanPostProcessor + IocContainer 生命周期（init 先于 postProcessor，close 逆序销毁）
- [x] M1-6 ToolRegistry + @Tool 捕获（重名报错；IocContainer.getToolRegistry()）
- [x] M2-1 ParameterSchemaGenerator：方法参数 → JSON Schema（type/description/required/enum），父 pom 开启 -parameters
- [x] M2-2 FileTools：read_file / write_file / edit_file + 工作区路径约束（拒绝绝对/越界）
- [x] M2-3 BashTool：bash → BashResult(stdout/stderr/exitCode)，并发读流防死锁，OS 检测选 cmd/sh
- [x] M2-4 SearchTools：glob / grep / search_files，遍历限定工作区
- [x] M2-5 MyccApplication 演示：装配容器 → start → 打印 7 个工具 + Schema
- [x] M2-6 `mvn clean install` 全绿（50 测试）；exec:java 启动打印 7 个工具
- [x] IocContainer 增加 `register(Class<?>...)` 便捷重载

## 待办事项
- [ ] M3：LLM 抽象 + MockProvider + OpenAiCompatProvider
- [ ] M4：Agent 循环 + 流式输出（mycc-ui InteractionPort + ToolCall/ToolResult + ToolCallExecutor）
- [ ] M5：存储抽象 + 会话落盘
- [ ] M6：CLI（JLine + picocli）+ fat-jar

## 阻塞与风险
- 无（功能层面）。Windows 控制台 System.out 中文按系统码页（GBK）输出，git-bash 下显示乱码属显示问题，M6 CLI 需显式设置 UTF-8 输出编码。

## 明日计划
- 进入 M3：LlmProvider 接口 + 流式回调 + MockProvider 离线端到端 + OpenAiCompatProvider
