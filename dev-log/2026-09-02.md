# 2026-09-02

## 今日完成
- [x] 与用户讨论思考内容（reasoning/thinking）显示的 4 个问题，确认改进方向：视觉区分 + 修丢数据 bug + 显示开关（M6 再做 ANSI，本期不加 ANSI）
- [x] 修 bug：`OpenAiCompatProvider.parseStream` 收尾用 2 参构造丢弃 reasoning，已改为 `new ChatResponse(content, reasoningContent, toolCalls)`；新测试断言 reasoning 保真、chunk 含 `reasoning_content`
- [x] `ConsolePort` 视觉区分 + `showReasoning` 开关（默认 false 隐藏思考）：开启时 THINKING 以 `[思考] ` 前缀开块、流式追加、下一个非思考事件（TOKEN/工具/错误/DONE）换行收块；3 测试（默认隐藏/开启分块显示/工具事件前收块）
- [x] `AgentDemoApp` 从 `ConfigService` 读 `showReasoning`（系统属性 `mycc.showReasoning` / 环境变量 `MYCC_SHOWREASONING` / `~/.mycc/config` 可覆盖，默认 false）传入 ConsolePort
- [x] `MockProvider` 支持流式 reasoning chunk（与真实 provider 对齐），新增 AgentLoop 测试：THINKING → TOKEN → DONE 事件序列
- [x] 移除 AgentLoop 中注释掉的 `printHistory` 死代码
- [x] `mvn clean install` 全量验证全绿（106 测试 = 28 core + 21 tools + 10 ai + 19 storage + 24 agent + 4 app）
- [x] `Storage.lastModified(key)` + `SessionStore.list()`：历史会话概况（`SessionSummary`，按修改时间倒序，title=最后一条 USER 消息），过滤 `session/latest` 指针
- [x] `AgentLoop` 改为显式绑定 session（移除「storage 非空自动恢复 latest」），工厂新增 7 参重载
- [x] 新增 `SessionMenu`（编号菜单 + 约 20 字截断 + 「新的对话」项 + 非法输入重读/Ctrl+D 兜底）与 `printHistory`（角色标记 `我 >`/`[工具]`/`[结果]`/`助手 >`）
- [x] `AgentDemoApp` 接入：无历史直接新会话；有历史先选菜单、打印历史再续聊；每轮结束照旧落盘
- [x] 提交在改内容：`feat(app)`（session menu + SessionStore.list + Storage.lastModified + AgentLoop 显式绑定会话）与 `docs(standards)`（注释规范改为详尽注释）
- [x] 按新注释规范为全部 50 个 `src/main` 生产文件补齐中文注释（类/方法 @param@return@throws/成员变量/常量枚举），纯注释改动、零逻辑变更；`mvn clean install` 全绿验证
- [x] 会话历史菜单重构（与用户确认方向）：历史回放与实时渲染走**同一事件协议**——新增 `USER` 事件类型（`OutputEventType`），`ConsolePort` 加 `我 > ` 渲染分支
- [x] mycc-agent 新增 `SessionPicker` 接口（`selectMenu(store)` 选择会话，依赖倒置：接口在 agent 侧、实现在 UI 侧）+ `SessionReplayer`（把历史 `Message` 序列回放为 USER/TOOL_CALL/TOOL_RESULT/TOKEN/DONE 事件流）+ 共享 `ToolCallFormatter`（历史/实时工具调用文本统一格式）
- [x] `ConsolePort` 改为实现 `InteractionPort` + `SessionPicker` 双端口（吸收 `SessionMenu.select/truncate`，新 3 参构造注入 `BufferedReader`）；删除 `SessionMenu` 与 `SessionMenuTest`，菜单测试并入 `ConsolePortTest`
- [x] `AgentDemoApp` 经 `SessionPicker` 选会话、`SessionReplayer` 回放历史、实时用户输入以 `USER` 事件渲染；`mvn clean install` 全绿（126 用例 = 28 core + 21 tools + 10 ai + 21 storage + 34 agent + 12 app）

## 待办事项
- [x] M6：CLI（JLine + picocli）+ fat-jar；思考块用 ANSI 灰色/斜体样式区分

## 阻塞与风险
- Windows 控制台 GBK 显示乱码依旧只是终端显示问题；M6 CLI 需显式 UTF-8。
- reasoning 目前不落盘（会话历史只存正文与工具结果），如需复盘思考过程再议。

## 明日计划
- 跑 `mvn clean install` 验收本期改动；进入 M6：mycc-cli（CliPort）+ fat-jar

## M6：CLI 界面 + fat-jar 启动器（实现完成）

### 今日完成
- 新增 mycc-cli 模块：CliPort（JLine 渲染 + ANSI 全事件分色，非 TTY 降级）、ReplLoop（/exit /clear + 逐轮异常容忍）、picocli 命令面（mycc / resume / sessions / tools / config）。
- mycc-app 收敛为纯装配器 Main + shade 单 fat-jar（ServicesResourceTransformer 保 JLine ServiceLoader）。
- 删除 ConsolePort / AgentDemoApp / SessionPicker；渲染用例迁入 CliPortTest。
- SessionStore.SessionSummary 扩展 lastModified，sessions 命令展示修改时间。
- Main 关键坑位修复：JLine `terminal.writer()` 为缓冲 PrintWriter，`System.exit` 不触发 close/autoflush，命令 println 输出会丢 → 退出前显式 `out.flush()`；无 OPENCODE_KEY 时只读命令（--help/sessions/tools/config）免 key 可用，仅裸 mycc / resume 引导退出非 0。

### 验证
- mvn clean install 全绿（新增 mycc-cli 三组测试：CliPortTest 8 / ReplLoopTest 7 / MyccCommandTest 9；ConsolePortTest 迁/删后 app 测试相应更新）。
- java -jar mycc-app/target/mycc-app.jar 手动冒烟通过：建新会话对话（「我 > 」回显 + 思考 + 工具调用 + /exit 退出 0）、resume 续最近回放历史、resume <指定 id>、resume <非法 id> 报「找不到会话」退出码 1、sessions 列表、tools 7 个工具、config 打印 showReasoning；重定向 `tools/sessions > out.txt` 无 `\033` 转义码（非 TTY 降级生效）。

### 待办事项
- 会话重命名 / 删除 / 搜索（v1 范围外，后续）。
- Web UI（v2）。

### 阻塞与风险
- 无。