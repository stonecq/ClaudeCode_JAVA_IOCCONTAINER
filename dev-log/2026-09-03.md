# 2026-09-03

## 今日完成
- [x] 规划 v2 阶段：保留 PRD + 新建 `docs/plans/2026-09-03-v2-development-plan.md`（M7–M13 路线图 = Hook → 权限 → Memory → Skill → Planning → Subagent → Web），M7 细化到任务级
- [x] 文档整理区分 v1/v2：v1 计划加「✅ 已完成」横幅 + DoD 勾选；PRD 状态行与 §7 标注 v1 完成/v2 进行中；`docs/README.md` 新增版本状态表；`CLAUDE.md` 同步 v2 阶段（简介/标准文档表/纪律 #1 M7→M13/模块地图 mycc-web 标 M13）
- [x] M7 核心（mycc-core `hook` 包）：`HookEventType`（6 事件枚举 + `eventName()`/`fromName()`）、`HookEvent`（type/sessionId/payload record）、`HookDefinition`、`HookRegistry`（BeanPostProcessor，EnumMap 按事件分组，签名校验启动期报错）、`HookDispatcher`（按注册顺序同步派发，单钩子异常仅 warn 不中断）
- [x] M7 装配：`IocContainer` 新增 `getHookRegistry()` + 构造挂入 hookRegistry 后置处理器；`AgentLoop` 新增 9 参工厂/构造（含 `HookDispatcher`，null=不触发钩子），埋点 session_start / session_end / user_prompt_submit / tool_call_before / tool_call_after / error
- [x] TDD：先写失败测试（HookEventTypeTest/HookRegistryTest/HookDispatcherTest/IocContainerHookTest/AgentLoopHookTest + fixture RecordingHook/BadEventHook/BadSignatureHook/OrderedHooks/HookRecorder）→ 实现 → 全绿
- [x] `mvn clean install` 全量验收全绿（39 core + 21 tools + 10 ai + 21 storage + 37 agent + 24 cli + 1 app；较 v1 126 增加 11 个 Hook 用例 → 137+）

## 待办事项
- [ ] M8：权限管理（工具调用审批流 + 决策持久化，走 M7 的 tool_call_before 钩子）

## 阻塞与风险
- `mvn clean install` 首次因 Windows 文件锁（`mycc-app/target/mycc-app.jar` 被占用）失败，非代码问题；句柄释放后 clean 正常，全量构建 BUILD SUCCESS。
- HookDispatcher 当前仅同步执行；FR-8 的「异步执行」留待有需求时再扩展（YAGNI）。

## 明日计划
- 进入 M8：权限管理——审批流（自动允许/拒绝/询问/规则）+ 高风险工具默认审批 + 决策持久化；先写 M8 详细 design/plan 再 TDD 实现。
