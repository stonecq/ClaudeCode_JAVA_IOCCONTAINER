# 2026-09-01

## 今日完成
- [x] 提交 M4 收尾：`feat(app): interactive agent demo with DeepSeek provider`（37b0971）
- [x] M5-1 新建 mycc-storage 模块（依赖 core + jackson）：`Storage` SPI（write/read/delete/keys）+ `FileStorage`（key→根目录相对路径，自动建父目录；拒绝空 key/`..`/绝对路径越界/NUL，默认根 `~/.mycc/`）；13 测试（读写/覆盖/嵌套目录/缺失读/删除/列 key/非法路径）
- [x] M5-2 `ConfigService`：加载 `~/.mycc/config`（key=value），覆盖优先级 系统属性 `mycc.<key>` > 环境变量 `MYCC_<KEY>` > 配置文件 > 默认值；6 测试（默认值/缺失/文件加载/文件覆盖默认/系统属性覆盖文件/缺失配置）
- [x] M5-3 `SessionStore`（mycc-agent 依赖 mycc-storage）：Conversation 消息历史（含工具调用与结果）序列化 JSON 存 `session/<id>.json`，按 id 原样恢复；5 测试（往返一致/物理文件布局/缺失 id/恢复后可转 ChatMessage/空工具调用往返）
- [x] `mvn clean install` 全绿（95 测试 = 28 core + 21 tools + 9 ai + 19 storage + 17 agent + 1 app）
- [x] SessionStore 接入 AgentLoop（保存 + 自动恢复）：save 写最近指针 `session/latest` + 新增 `latest()`；AgentLoop 构造注入 SessionStore（null=不持久化，旧构造/工厂兼容），启动自动恢复最近会话，每轮 run() finally 落盘；AgentDemoApp 用 `FileStorage.defaultDirectory()`（~/.mycc/）接入并打印恢复提示。新增 3 测试（每轮落盘/构造恢复续聊/无存储不落盘），全绿 101 测试 = 28 core + 21 tools + 9 ai + 19 storage + 23 agent + 1 app

## 待办事项
- [ ] M6：CLI（JLine + picocli）+ fat-jar；`/new` 新会话等会话管理命令

## 阻塞与风险
- Windows 控制台 GBK 输出乱码依旧只是显示问题（surefire 断言信息可见乱码，测试本身按 Unicode 判断），M6 CLI 需显式 UTF-8。
- Jackson 反序列化 record `Message` 依赖编译器 `-parameters`（父 pom 已开），依赖类路径时需保证同参数编译。
- `FileStorage.rejectsAbsolutePath` 首测误用根内路径导致不抛异常，已改为根外路径断言。

## 明日计划
- 进入 M6：mycc-cli（CliPort 实现 InteractionPort，JLine + picocli）+ mycc-app 启动器绑定 CLI + shade fat-jar；AgentLoop 会话落盘接入
