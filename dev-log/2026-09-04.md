# 2026-09-04

## 今日完成
- [x] **M8 权限管理（工具调用审批流）**：设计文档 `docs/superpowers/specs/2026-09-04-m8-permission-design.md` + 实现文档 `docs/superpowers/plans/2026-09-04-m8-permission.md`。审批走 M7 `tool_call_before` 钩子 + `HookDecision` 否决，**零 AgentLoop/CliContext 改动、零新增模块依赖**。
- [x] M8-1 风险属性（core）：`ToolRisk` 枚举（LOW/HIGH）、`@Tool` 新增 `risk()`（默认 LOW）、`ToolDefinition` 增 `risk` + 5 参构造（保留 4 参委托，既有调用点免改）、`ToolRegistry` 透传。
- [x] M8-2 权限模型（core）：`PermissionVerdict` / `PermissionDecision`（record verdict+reason）/ `PermissionRuleStore` 接口 / `PermissionPolicy`（纯决策：规则命中返规则，否则 HIGH→ASK / LOW→ALLOW，不引用 ToolCall）/ `UserConfirmation` 接口 + `ConfirmChoice`。
- [x] M8-3 规则存储（storage）：`JsonPermissionRuleStore`（`@Component`，会话内存 Map + 持久 `<workspace>/.mycc/permissions.json`；remember 写双份、resolve 先会话后项目、clearSession 只清内存；含 no-arg 与 ApplicationConfig 双构造）。
- [x] M8-4 工具风险声明（tools）：`bash` / `write_file` / `edit_file` 标 `ToolRisk.HIGH`，只读工具保持 LOW；`ToolRiskDeclarationTest` 断言。
- [x] M8-5 CLI 审批（cli）：`CliPermissionPrompt`（`[y]允许本次 [N]拒绝 [a]始终允许`，空/EOF/IO→UNAVAILABLE 降级拒绝，非法字符重询），7 用例全绿。
- [x] M8-6 审批钩子（hooks）：`PermissionHook`（手工构造非 @Component；TOOL_CALL_BEFORE 决策 + SESSION_START 清内存；未知工具按 HIGH 兜底、headless fail-closed 拒绝），11 用例全绿。
- [x] M8-7 装配（app）：`Main` 接线 `JsonPermissionRuleStore` + `PermissionPolicy` + `CliPermissionPrompt` + `PermissionHook`，经 `hookRegistry.postProcessAfterInitialization` 手工注册；`mvn -pl mycc-app -am test` 通过。
- [x] M8-8 端到端（agent）：`PermissionFlowTest`（真实 PermissionHook + JsonPermissionRuleStore@TempDir + BashTool）4 用例——ALLOW_ONCE 执行不持久化 / DENY 回填 `被钩子拦截: 用户拒绝调用: bash` / ALLOW_ALWAYS 执行并落盘 `"bash":"ALLOW"` / headless 回填 `被钩子拦截: 审批环境不可用，拒绝高风险调用: bash`。
- [x] `mvn clean install` 全量验收全绿（core 49 + tools 18 + ai 10 + storage 28 + hooks 18 + agent 43 + cli 31 + app 1 = **198 用例**，较 M7 结束 158 净增 40）。shade fat-jar `java -jar mycc-app/target/mycc-app.jar tools` 运行正常（验证容器装配 JsonPermissionRuleStore 无回归，只读命令免 key）。

## 待办事项
- [ ] M8 交互式人工检查（需 `OPENCODE_KEY` + TTY）：`bash(cmd="echo hi")` 触发 `[y/N/a]` → `y` 放行执行、`N` 拦截回填、`a` 后核验工作区 `.mycc/permissions.json` 含 `"bash":"ALLOW"`；新开会话验证内存缓存清空、持久规则仍生效
- [ ] M9：Memory 长期记忆（用户/项目/会话三层读写、注入提示词、按关键词检索，基于 Storage SPI）

## 阻塞与风险
- 无代码阻塞。交互式审批需真实 LLM key + TTY，自动化已由 PermissionFlowTest 覆盖四条审批路径。
- 决策优先级与会话缓存语义（SESSION_START 清内存、持久文件跨会话生效）后续 M9 复用同套容器/存储模式时需保持一致。

## 明日计划
- 进入 M9 Memory 长期记忆：先细化设计（三层记忆模型 + 注入/检索机制）再 TDD 实现；参考 M8 走「core 纯机制 + storage 持久化 + cli/agent 接线」的模块切分。