# 2026-09-09

## 今日完成
- [x] **M9 二期：记忆索引 + 按需调取 + 回合自动保存**（以提问方式敲定 4 个设计决策：分层索引 / read·save·delete 工具集 / 主动保存+会话自动 / 会话层单条整体）。
- [x] **MemoryIndex（新）**：纯字符串索引工具（`- id: 描述` 行格式），`upsert`（同 id 更新描述保持位置 / 追加）、`remove`（清空返回空串）。7 单测。
- [x] **MemoryStorage 改造**：构造注入 `ApplicationConfig` 定位当前工作区；新增 USER/PROJECT 层的 `loadIndex / load / save / delete`（PSESSION 传入抛异常），SESSION 层专用 `loadSession / saveSession`（单条，不走索引）。PROJECT 层按 `memory/project/<enc(ws)>/` 命名空间隔离不同项目。旧 `saveMemory/loadMemory` 移除，不留 shim。17 单测。
- [x] **MemoryTools（新）**：`read_memory / save_memory / delete_memory` 三 `@Tool`（`@Component`，SESSION 层拒绝），承接「看索引→按 id 调取→写入」闭环。7 单测；经 ToolRegistry（BeanPostProcessor）自动装配，`MyccApplicationTest` 内置工具集合断言 7→10 佐证。
- [x] **MemoryHook 改造**：SESSION_START 注入「会话全文 + USER 索引 + PROJECT 索引」（每层带引导标题，空层跳过）；新增 SESSION_END 订阅 `saveTurnMemoryHook`，把回合记录写回会话层。6 单测。
- [x] **AgentLoop.run（agent 模块）**：SESSION_END payload 由 null 改为「用户: …\n回答: …」String（不引入跨模块类型，`mycc-agent` 主代码仍不依赖 mycc-memory）。
- [x] **测试更新**：重写 `AgentMemoryInjectionTest`（新 API + 索引注入断言 + 回合落盘端到端），`AgentLoopHookTest` 事件序列不受影响。
- [x] **M10 Skill 一期（注解方式 + Invoke）**：core 新增 `@Skill` 注解 + `SkillDefinition` + `SkillRegistry`（BeanPostProcessor，与 Tool/Hook 同构），`IocContainer` 挂 skillRegistry 三件套齐；新建 `mycc-skill` 模块（`SkillHook` SESSION_START 注入技能目录 + `SkillTools.invoke_skill` @Tool + `CodeReviewSkill` 示例）；`AgentLoop` 零改动、装配零改动（技能类 `@Component`+`@Skill` 自动进容器）。SKILL.md 文件加载留二期。
- [x] **M10 测试**：`SkillRegistryTest`（7，core）+ `SkillHookTest`/`SkillToolsTest`（mycc-skill 5）+ `SkillAssemblyTest`（agent 端到端：目录注入 + invoke_skill 取回指令 2）；`MyccApplicationTest` 工具集合断言 10→11。
- [x] **M10 二期（SKILL.md 文件加载）**：core 的 `SkillRegistry` 新增 `override`（同名替换，供"项目覆盖全局"）；mycc-skill 新增 `SkillFileParser`（frontmatter 键值解析，零 YAML 依赖，无成对 `---` 按全文即指令）与 `SkillFileLoader`（`@Component`+`InitializingBean`，容器启动时先扫全局 `~/.mycc/skills` 唯一注册、再扫项目 `<workspace>/.mycc/skills` 覆盖注册；空目录静默跳过、不热更新）。文件技能与注解技能共享注册表，`invoke_skill`/技能目录自动覆盖，零额外接线。
- [x] **M10 二期测试**：`SkillFileParserTest`（5）+ `SkillFileLoaderTest`（6：全局+项目叠加/项目覆盖/忽略无 SKILL.md 目录/空根不崩/与已有注册同名快速失败/组件空根加载）+ `SkillRegistry` override 3 测；修正了"全局内同名"用例（文件系统同名目录不可共存，改测"全局与已注册注解同名"真实语义）。
- [x] **M11 Planning 一期（工具驱动）**：core 新增 `ToolContext` 工具上下文注入（record + `ParameterSchemaGenerator` 跳过该参数 + `ToolCallExecutor` 执行时注入当前 sessionId——工具首度感知会话，为 Subagent 复用打基础）；agent 新增 `planning` 包：`Plan`/`PlanStep`（不可变 record，markDone/allDone/nextIndex）、`PlanStore`（按 sessionId JSON 落盘 `plan/<id>.json`）、`PlanTools`（`create_plan`/`complete_step` 两 @Tool，步骤用多行 String 参数）；`AgentLoop.handleToolCalls` 改传 `ToolContext(session.id())`。计划文本经工具结果自然进入消息历史，LLM 逐步推进，无需注入 Hook。
- [x] **M11 测试**：core `ParameterSchemaGeneratorTest`/`ToolCallExecutorTest` 扩展（schema 跳过 ToolContext + 执行注入）；agent planning `PlanTest`(4)/`PlanStoreTest`(4)/`PlanToolsTest`(6)/`PlanFlowTest`(端到端：建计划→逐步 complete_step→全部完成)；`MyccApplicationTest` 工具集合断言 11→13。
- [x] **M11 收尾（计划完成清理）**：`PlanStore` 新增 `delete`；`complete_step` 全部完成时删除计划文件（`allDone` 判定后直接删、不先 save），端到端确认同会话不再残留。修复"计划文件残留"缺口：避免随会话累积的垃圾文件 + 续聊读到旧已完成计划的语义混乱。
- [x] **M11 补充（read_plan 读取计划）**：`PlanTools` 新增 `read_plan` @Tool（修正用户草稿的 `read_tool` 命名），无计划时返回提示文本而非抛错（查询无结果非失败）；补 3 单测（有计划全文 / 无计划提示 / 全完成回落提示），`MyccApplicationTest` 工具断言 13→14。
- [x] **全量回归**：`mvn clean install` 通过，12 模块 317 测试全绿。

## 待办事项
- [ ] `decodeProjectPath` 除编解码往返测试外仍无调用方，按 YAGNI 决定去留。
- [ ] 记忆工具的权限：`@Tool` 默认 LOW 未排审批，`delete_memory` 有破坏性，是否设更高风险待定。
- [x] CLI 实机验证（部分）：已实测大模型会调用 `save_memory` 工具。
- [ ] CLI 实机验证：会话开始 SYSTEM 消息含三层记忆索引文本（用户/项目索引 + 会话全文），另确认回合结束 SESSION_END 实际落盘会话记忆。
- [ ] **记忆清理（已排期）**：数量阈值检测 + 回合结束触发清理，已列入 v2 计划「v2 后续增强 → M9b」，触发点/策略待启动时细化。

## 阻塞与风险
- 无阻塞。
- 已知边界：SESSION 层注入只在 SESSION_START（新建空会话）触发，同会话续聊不会重复注入（上下文由历史消息承载）；会话记忆是「用户输入 + 最终回答」的原文记录，**非语义摘要**（选定"不额外调 LLM"的自然结果）。

## 明日计划
- 处理 `decodeProjectPath` 去留与记忆工具权限等级。
- CLI 实机验证记忆注入与工具调用链路。
- M10 收尾可选：CLI 实测技能目录注入与 invoke_skill 调用；SKILL.md 文件加载（M10 二期）启动时再细化。
- M11 Planning 规划或按用户节奏推进。