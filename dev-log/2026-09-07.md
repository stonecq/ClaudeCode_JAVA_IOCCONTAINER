# 2026-09-07

## 今日完成
- [x] **Spring 化重构 S1（权限/存储/钩子注解化）**：`HookDispatcher` 加 `@Component`+`@Inject`、`PermissionHook` 加 `@Component`（依赖按 4 参构造自动注入）、`ConfigService` 加 `@Component`、新 `StorageConfig`（`@Configuration`，`@Bean FileStorage`）、`SessionStore` 加 `@Component`+`@Inject Storage`、`MyccApplication` 拆出 `start()`（ctor=create+register 仅）、`Main` 删 `PermissionHook` 手工 postProcess 块改从容器取 `HookDispatcher`。
- [x] **修复 BeanFactory 接口回退 bug（S1 暴露）**：`resolveAssignable` 对已按具体类型实例化的单例在定义侧重复 `createBean`，导致接口注入误报「多个可匹配」。改为经 `getBean(具体类型)` 复用缓存；新增 `BeanFactoryTest.interfaceFallbackReusesExistingSingleton` 回归用例（core 67→68）。
- [x] **S2 CLI/AI/Agent 装配迁移**：新 `AiConfig`（`@Bean LlmProvider`，缺 OPENCODE_KEY→`UnavailableLlmProvider` 兜底）+ `UnavailableLlmProvider`；新 `AgentConfig`（`@Bean @Scope(PROTOTYPE) agentLoop`，model/maxIterations 读 ConfigService → 消灭装配根硬编码）；新 `CliConfig`（`@Bean` Terminal/PrintWriter/LineReader/LineInput/CliPort/UserConfirmation/*PROTOTYPE ReplLoop/CliContext，`CliContext.setContainer`）；5 个命令加 `@Component`；`Main` 全瘦身为「开容器 → start → 执行命令 → close」，聊天命令缺 key 预检引导退出非 0。
- [x] **修复 JLine 终端启动问题（S2 暴露）**：`TerminalBuilder.system(true)` 在 Windows 无控制台时逐个探测原生终端耗时 4-7s 且残留线程阻塞 JVM 正常退出（app 测试 31s 挂起）。`CliConfig.terminal()` 改无控制台（`System.console()==null`）时直接建 `DumbTerminal`——只读命令与测试秒启、干净退出（MyccApplicationTest 31s→0.9s）。
- [x] **S2 验收**：`mvn -pl mycc-app -am test` 与 `mvn clean install` 全绿；`MyccApplicationTest` 新增 `cliUserConfirmationShadowsUnavailable`（UserConfirmation 精确命中 CliPermissionPrompt，遮蔽 Unavailable fail-closed）。
- [x] **人工 jar 验收（只读 + 缺 key 路径）**：`tools/sessions/config/--help` 只读命令正常；`env -u OPENCODE_KEY` 下裸 `mycc`/`resume` 输出引导文案并退出码 1。交互 REPL 需 TTY+key，本环境无法验证。
- [x] **消除装配双路径 + 注入替身能力（S3 后续）**：新增 `BeanFactory.overrideSingleton(Class,Object)`（写入单例缓存、`getBean` 优先返回、遮蔽已有定义；`registerSingleton` 维持严格冲突契约，等价 Spring `@MockBean`），接口/IoC 委托 + 2 条回归。
- [x] **`CliContext.enterRepl` 收单一路径**：删除 `container==null` 的手工直装分支（`AgentLoop.withToolRegistry` + `new ReplLoop`），未装配容器即抛错；配合 CliContext 改为 `@Component` + 字段注入 + `InitializingBean` 读 model/maxIterations（`CliConfig` 移除 `cliContext` @Bean）。
- [x] **`MyccCommandTest` 迁移到容器装配**：helper 建真实容器 + `overrideSingleton` 注入测试替身（temp 存储/配置、StringWriter 输出、StringReader 输入、MockProvider），注册 `{CliContext, HookDispatcher, AgentConfig, CliConfig}`，`getBean(CliContext)` 取出——测试驱动与生产完全同路径，3 个失败用例恢复绿。
- [x] **新增 `MyccApplicationTest.enterReplContainerPathWiresPrototypes`**：真实容器 `getBean(AgentLoop, session)` args 覆盖绑定断言 + `getBean(ReplLoop, args)` 全参覆盖驱动一行，把生产原型 wiring 纳入回归。
- [x] **`ReplLoop` 转 prototype `@Component`**（用户拍板）：类标 `@Component @Scope(PROTOTYPE)` + `@Inject` 单参构造，删除 `CliConfig` 的 `replLoop` @Bean 与 @Scope 相关 import（避免重复注册）；同时清理 `CliConfig` 残留的 6 个无用 import（cliContext 工厂移除后遗留）。
- [x] **`CliContext` 去残留**（用户拍板）：删除已无调用点的 `setContainer`、两个测试缝构造器、`model/maxIterations` 字段 + `afterPropertiesSet`/`InitializingBean`——回归纯 `@Component` + 字段注入形态，enterRepl 唯一装配路径。
- [x] 全量 `mvn clean install` 绿：core 70 / tools 18 / ai 10 / storage 28 / hooks 18 / agent 43 / cli 31 / app 3 = **221 用例**（较 S3 初 218 净增 3，本次改动用例数不变）。
- [x] **修复 opencode 网关 400 MissingSessionID**：`x-opencode-session` 是随请求的会话路由头，非静态连接配置。方案（用户拍板）：`ChatRequest` 增 `sessionId`（`of(model,messages)` 保留 + `withSessionId`），`AgentLoop.buildRequest` 每轮带 `session.id()`，`OpenAiCompatProvider` 非空即设头。撤掉半套的 `ModelConfig(headers)` 静态 map 与 provider 拼头逻辑（YAGNI），`AiConfig` 恢复 `llmProvider()` + 常量基址、清空 Map/Named 误加 import。`OpenAiCompatProviderTest` +2 断言（有 sessionId 发头 / 无则不发）。全量 **223 用例**绿。
- [x] 注：本环境无法对真实 opencode 网关跑交互验证——结构修复 + header 单测已证头会带上；真实 400 是否消除需用户在真机跑一轮 chat 确认（若 opencode 只认自己签发的 session，需另行探讨 id 语义）。
- [x] **厂商头解耦（用户拍板：子类 + 钩子）**：`OpenAiCompatProvider` 回归纯 OpenAI 兼容（`final` 放开、加 `protected applyVendorHeaders(builder, request)` 默认 no-op，不内建任何厂商头）；新 `OpenCodeGatewayProvider extends OpenAiCompatProvider` 把 `x-opencode-session` 编码 conversationId；`ChatRequest.sessionId` 更名中性 `conversationId`（`withConversationId`），`AgentLoop` 照常携带。`AiConfig` 指向子类；换普通 OpenAI 兼容端点用基类即可（0 代码）。测试迁移：基类断言「即使带 conversationId 也不发厂商头」，opencode 头断言进新 `OpenCodeGatewayProviderTest`。全量 **224 用例**绿。

## 待办事项
- [ ] **S3 文档收尾（本会话内）**：设计文档实施记录 + v2 计划/design-standards 更新 + 本 build 全量复检。
- [ ] M8 交互式人工检查（需 `OPENCODE_KEY` + TTY）：`bash(cmd="echo hi")` 触发 `[y/N/a]` → `y` 放行 / `N` 拦截回填 / `a` 后核验工作区 `.mycc/permissions.json`；新开会话验证内存缓存清空、持久规则仍生效（S2 后审批由容器内的 `CliPermissionPrompt` 承接，需真机复核）。
- [ ] M9：Memory 长期记忆（用户/项目/会话三层读写、注入提示词、按关键词检索，基于 Storage SPI）。

## 阻塞与风险
- 交互 REPL 审批路径在无 TTY 环境无法自动化验证，M8 交互检查与审批真机复核留待用户带 key+TTY 环境人工完成。
- 设计文档 §5 AgentConfig 示例形参写 `CliPort`，实际 `AgentLoop.withToolRegistry` 签名是 `InteractionPort`（mycc-agent 不依赖 mycc-cli、依赖 mycc-ui）；按实际签名实现。
- JLine `TerminalBuilder` 在 Windows 无控制台探测原生终端的 4-7s 延迟与线程残留，已用「无控制台直建 DumbTerminal」规避；真实交互终端行为需人工复核。

## 明日计划
- 收尾 S3 文档后，经用户确认进入 M9（Memory 长期记忆）：基于 Storage SPI 实现用户/项目/会话三层记忆读写 + 注入提示词 + 关键词检索，先设计文档后分模块执行。